<h1>Basket items are :</h1>
<br/><isloop items="${pdict.Basket.getProductLineItems()}" var="productLineItem" status="loopState">
${loopState.count}
${loopState.index}

    ${productLineItem.product.name}<br/>
</isloop>